gold = int(input("Enter Total Gold: "))
player_number = int(input("How much player do you want in each squad: "))
power = 0
squad_power_a = 0
squad_power_b = 0
add_gold = 0
print()
print(f"==== Building Squad A ====")
print()
for i in range(1 , player_number + 1):
    player_added = False
    while not player_added:
        player_name = (input(f"Player {i} Name: "))
        bid_price = int(input(f"Player {i} Bid Price: "))
        if bid_price > gold:
            print(f"Insufficent Gold! | Left Gold: {gold}")
            choice = input(f"Gold Isn't enough.\n Add more Gold or choose another Player for bid(ADD/CHOOSE): ").upper()
            if choice =="ADD":
                extra = int(input("How much extra gold you want to add: "))
                gold += extra
                print(f"Your Gold Balance: {gold}")
        else:
            gold -= bid_price
            player_added = True
            role = input("Role: ")    
            if role == "Sniper":
                power = 100 
                print(f"Player {i} Power:{power} | Gold Left: {gold}")
            elif role =="Rusher": 
                power = 80
                print(f"Player {i} Power: {power} | Gold Left: {gold}")
            else:
                power = 50
                print(f"Player {i} Power: {power} | Gold Left: {gold}")
        squad_power_a += power    
print()
print(f"Squad A Total Power:{squad_power_a}")        
       
print()
print("==== Building Squad B ====")
print()
for i in range(1 , player_number + 1):
    player_added = False
    while not player_added:
        player_name = (input(f"Player {i} Name: "))
        bid_price = int(input(f"Player {i} Bid Price: "))
        if bid_price > gold:
            print(f"Insufficent Gold! | Left Gold: {gold}")
            choice = input(f"Gold Isn't enough.\n Add more Gold or choose another Player for bid(ADD/CHOOSE): ").upper()
            if choice == "ADD":
                extra = int(input("How much extra gold you want to add: "))
                gold += extra
                print(f"Your Gold Balance: {gold}")
        else: 
            gold -= bid_price
            player_added = True        
            role = (input(f"Role: "))
            if role == "Sniper":
                power = 100 
                print(f"Player {i} Power:{power} | Gold Left: {gold}")
            elif role =="Rusher":
                power = 80
                print(f"Player {i} Power: {power} | Gold Left: {gold}")
            else:
                power = 50
                print(f"Player {i} Power: {power} | Gold Left: {gold}")
    squad_power_b += power    
print()
print(f"Squad B  Total Power:{squad_power_b}")        
print()
print("--- TOURNAMENT PREDICTION ---")
if squad_power_a > squad_power_b:
    print(f"Winner: Squad A power ( {squad_power_a} VS {squad_power_b}")
    print(f"Remaining Gold: {gold}")
elif squad_power_a < squad_power_b:
    print(f"Winner: Squad B power ({squad_power_b} VS {squad_power_a})")
    print(f"Remaining Gold: {gold}")
else:
    print(f"POWER OF BOTH TEAMS ARE EQUAL\n CONGRATS! TO BOTH TEAMS.")

    

    
            

    
